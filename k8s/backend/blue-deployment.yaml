apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-backend-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ecommerce-backend
      version: blue
  template:
    metadata:
      labels:
        app: ecommerce-backend
        version: blue
    spec:
      containers:
      - name: ecommerce-backend
        image: ecommerce-backend:blue
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: postgres://admin:password123@postgres:5432/ecommerce
